# Village

Village imagery and sample glyphs illustrate the narrative. They are not a live registry of wallets or token holders. The UI may show verified wallet-linked numbered IDs only when an indexer implementing a published assignment rule returns them. A token balance alone must never be expanded into guessed Life IDs. Without that indexer, the site labels the relevant view unavailable or sample-only.
